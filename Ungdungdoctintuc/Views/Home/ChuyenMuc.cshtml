
@{
    ViewBag.Title = "ChuyenMuc";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="bg-categories">
    <nav class="navbar navbar-expand-sm bg-dark">
        <ul class="navbar-nav">
            @foreach (var item in Model)
            {
                <li class="nav-item"><a class="nav-link text-light" href="@Url.Action("ChuyenMuc", "Home", new { id = item.IdTheLoai })" style="margin:auto"><b> @item.TenChuyenMuc</b></a></li>
            }
        </ul>
    </nav>

    <div class="container">
        <div class="row">
            <div class="col-sm-8">
                <div class="new-post-title">
                    <h2>bài viết mới</h2>

                </div>
            </div>
            <div class="col-sm-4">
                <div class="new-post-title">
                    <h2>video mới</h2>
                </div>
            </div>
        </div>
        <div class="new-post-content">
            <div class="row">
                @Html.Action("ShowPart1Category1", "Home")
            </div>
        </div>
        <div class="list">
            <div class="row">

                <div class="col-sm-8 my-2">
                    <div class="my-2">
                        @Html.Action("ShowPart2Category2", "Home")
                    </div>
                </div>
                
                </div>
            </div>
        </div>
    </div>


